plugins {
    alias libs.plugins.android.library
    alias libs.plugins.jetbrains.kotlin.android
}

def gradlePath = "$rootDir/gradle"
apply from: "${gradlePath}/common.gradle"

android {
    namespace 'com.rawg.core'
}

dependencies {
    implementation libs.gson
}